const HeaderBanner = () => (
  <div className="w-full pt-1 text-center text-[0.7rem] font-semibold uppercase tracking-[0.45em] text-muted-foreground">
    Noticias de Buenos Aires. Sin filtros, Sin sesgos.
  </div>
);

export default HeaderBanner;
